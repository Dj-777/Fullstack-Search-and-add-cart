{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon app.js",
    "start": "node app.js",
    "prod": "concurrently \"pm2 start app.js\" \"cd client1 && npm run build && npm start\" ",
    "lint": "eslint \"**/*.js\"",
    "lint:fix": "eslint --fix \"**/*.js\"",
    "format:check": "prettier --check ./**/*.js",
    "format:write": "prettier --write ./**/*.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemon": "^3.0.2"
  }
}
